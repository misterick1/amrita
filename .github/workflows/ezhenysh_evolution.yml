import os
from solana.rpc.async_api import AsyncClient
from solders.keypair import Keypair # type: ignore

# Инициализируем боевые узлы бабочки
RPC_URL = os.getenv("SOLANA_RPC_URL") # Твой приватный RPC
client = AsyncClient(RPC_URL)

# Загружаем ключи узлов (секреты репозитория)
BODY_KEY = Keypair.from_base58_string(os.getenv("SERVER_1"))       # misterick18x108
WING_L_KEY = Keypair.from_base58_string(os.getenv("SERVER_2"))     # misterick1
WING_R_KEY = Keypair.from_base58_string(os.getenv("SERVER_3"))     # wagmisalmoneste
REMOTE_KEY = Keypair.from_base58_string(os.getenv("SERVER_4"))     # raredolphingree
