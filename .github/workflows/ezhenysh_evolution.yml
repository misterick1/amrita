name: "🦔 Ezhenysh Swarm Evolution - Emerald Edition"

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '*/15 * * * *'

jobs:
  evolution_loop:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install solana solders httpx

      - name: Run Swarm Core Logic
        # Пакуем абсолютно все секреты репозитория
        env:
          ALL_REPOS_SECRETS: ${{ toJson(secrets) }}
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
        run: python swarm_core.py

      - name: Run Pi Bridge Activation
        env:
          PI_API_KEY: ${{ secrets.PI_API_KEY }}
        run: python pi_bridge_activate.py
